{
  "name": "@tool/mock",
  "version": "0.7.0",
  "description": "Mock server for the web application",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clean": "rimraf dist",
    "clean:all": "pnpm clean node_modules",
    "dev:mocks:cli": "mocks-server",
    "test:e2e:env": "mocks-server --no-plugins.inquirerCli.enabled --log=silent"
  },
  "devDependencies": {
    "@config/eslint": "workspace:*",
    "@config/typescript": "workspace:*",
    "@mocks-server/main": "catalog:mocks"
  }
}
